{
  "name": "build-mermaid-bundle",
  "version": "1.0.0",
  "private": true,
  "description": "Builds a minimal mermaid.js parser bundle for use with Jint",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "mermaid": "^12.0.0"
  },
  "overrides": {
    "lodash-es": ">=4.18.0"
  }
}
